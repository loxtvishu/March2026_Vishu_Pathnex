# Day 07 - SPEED TYPING

Rewrite ALL FOUR from scratch:

1. Ansible: Install nginx

---
- name: Install Nginx for Pathnex Server
  hosts: all
  become: yes

  tasks:
    - name: Install Nginx
      package:
         name: nginx
         state: present
    
    - name: Ensure Nginx is Started and Enabled
      services:
         name: nginx
         state: started
         enabled: yes

2. Terraform: EC2 (choose any Pathnex instance)

terraform {
    required_providers {
        aws = {
            source = "hashicorp/aws"
            version = "~> 5.0"
        }
    }

    required_version = ">= 1.3.0"
}

provider "aws" {
    region = "ap-south-1"
}

resource "aws_instance" "pathnex_server" {
    ami = "ami-0abcd11234abcd1244"
    instance_type = "t2.micro"

    tags = {
        Name = "Pathnex-Server"
        Environment = "Development"
    }
}


1. Kubernetes: Deployment 2 replicas

apiVersion: apps/v1
kind: Deployment
metadata:
   name: pathnex-nginx-deployment
   labels:
     app: nginx
spec:
   replicas: 2
   selector:
     matchLabels:
       app: nginx
   template:
     metadata:
       labels:
         app: nginx
     spec:
       containers:
         - name: nginx
           image: nginx:latest
           ports:
             - containerPort: 80

2. Shell: Print date + uptime

#!/bin/bash
echo"Current Date and Time:"
date
echo
echo "System Uptime:"
uptime

Set a Timer: **20 minutes**

# Parameterized Builds

## 🔹 Jenkins Pipeline - Parameterized

You will learn how to **create parameterized builds in Jenkins**.

pipeline {
    agent any
    parameters {
        string(name: 'ENVIRONMENT', defaultValue: 'dev', description: 'Deployment Environment')
    }
    environment {
        INSTITUTE_NAME = "Pathnex"
    }
    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/loxtvishu/sample-java-app.git'
            }
        }
        stage('Deploy') {
            steps {
                sh 'echo "Deploying to $ENVIRONMENT by $INSTITUTE_NAME"'
            }
        }
    }
}

## 🔹 GitLab CI - Parameterized via Variables

You will learn how to **use variables to parameterize GitLab jobs**.

stages:
 - deploy

variables:
 INSTITUTE_NAME: "Pathnex"
 ENVIRONMENT: "dev"

deploy:
 stage: deploy
 image: alpine:latest
 script:
   - echo "Deploying to $ENVIRONMENT by $INSITUTE_NAME"

# Day 07 - Automating Cloud Infrastructure with Terraform  & Ansible 
- name: Setup Nginx with Custom Config
  hosts: all
  become: yes
  tasks:
    - name: Install nginx
      yum:
        name: nginx
        state: present
    - name: Copy custom nginx.conf
      copy:
        src: nginx.conf
        dest: /etc/nginx/nginx.conf
    - name: Start nginx service
      service:
        name: nginx
        state: started
        enabled: yes

## 🔹 Terraform - Provision EC2 with IAM Role

resource "aws_iam_role" "ec2_role" {
    name = "pathnex_ec2_role"
    assume_role_policy = jsonencode({
        Version = "2012-10-17"
        Statement = [
            {
                Action = "sts:AssumeRole"
                Effect = "Allow"
                Principle = {
                    Service = "ec2.amazonaws.com"
                }
            },
        ]
    })
}

resource "aws_instance" "Pathnex_ec2" {
    ami = "ami-0abcd1234abcd1234"
    instance_type = "t2.medium"
    iam_instance_profile = aws_iam_role.ec2_role.name

    tags = {
        Name = "Pathnex-EC2"
    }
}

## 🔹 Kubernetes - ConfigMap and Secret Management
apiVersion: v1
kind: ConfigMap
metadata:
   name: pathnex-config
data:
   app.properties: |
     key=value

---
apiVersion: v1
kind: Secret
metadata:
  name: pathnex-secret
type: Opaque
data:
  password: cGF0aG5leHBhc3M=

## 🔹 Shell Script - Archive Logs

#!/bin/bash
tar -czf /var/log/archived_logs.tar.gz /var/log/*.log

## 🔹 Docker Java Application

# JavaScript
class Hello {
    pubic static void main (String[] args) {
        System.out.println("Hello Pathnex");
    }
}

# Docker File
FROM openjdk:17
WORKDIR /opt/pathnex/java-app
COPY Hello.java /opt/pathnex/java-app/
RUN javac /opt/pathnex/java-app/Hello.java
CMD ["java", "-cp", "/opt/pathnex/java-app", "Hello"]