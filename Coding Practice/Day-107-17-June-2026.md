# Day 17 - Loops , ALB , Namespace , Top Processes

## 🔹 Ansible - Create Multiple Users in Loop

---
- name: Create multiple coder users
  hosts: all
  become: yes

  vars:
     users:
       - dev1
       - dev2
       - dev3

  tasks:
     - name: Create users
       user:
         name: "{{ item }}"
         state: present
       loop: "{{ users }}"


## 🔹 Terraform - Application Load Balancer

resource "aws_lb" "CodrALB" {
    name = "code-alb"
    load_balancer_type = "application"
    subnets = [aws_subnet_CodrSubnet.id]

    tags = {
        Name = "Codr-ALB"
    }
}

## 🔹 Kubernetes - Namespace + Deployment

apiVersion: v1
kind: Namespace
metadata:
   name: codr-namespace

---
apiVersion: apps/v1
kind: Deployment
metadata:
   name: codr-deploy
   namespace: codr-namespace
spec:
   replicas: 2
   selector:
     matchLabels:
        app: codr
   template:
     metadata: 
        labels:
          app: codr
     spec:
        containers:
          - name: app
            image: nginx



## 🔹 Shell Script - Top 5 Memory Process

#!/bin/bash

ps aux --sort=-%mem | head -n 6

# Deployment Simulation

## 🔹 Jenkins Pipeline - Simulated Deployment

You will learn how to **simulate deployment to different environments**.

pipeline {
    agent any 
        parameters {
            choice(name: 'ENVIRONMENT' , choices: ['dev' , 'staging' , 'prod'], description: 'Deployment Environment')
    }
    environment {
        INSTITUTE_NAME = "Codr"
    }
    stages {
        stage('Deploy') {
            steps {
                sh 'echo "Deploying $INSITUTE_NAME application to $ENVIRONMENT"'
            }
        }
    }
}

# Day 17 - Multi - Cloud Deployments, Auto-healing, and Advanced CI/CD

## 🔹 Ansible - Auto-healing Web Servers with Systemd

```yaml
- name: Configure Auto-healing for Web Server
 hosts: all
 become: yes
 tasks:
   - name: Ensure nginx service is running
     service:
       name: nginx
       state: started
       enabled: yes
   - name: Setup auto-restart for nginx service
     lineinfile:
       path: /etc/systemd/system/nginx.service.d/override.conf
       line: "Restart=always"
       create: yes
   - name: Reload systemd
     command: systemctl daemon-reload

🔹 Terraform - Multi-Cloud Infrastructure with AWS and Azure
# AWS VPC
resource "aws_vpc" "main" {
   cidr_block = "10.0.0.0/16"
}

# Azure VNet
resource "azurerm_virtual_network" "main" {
   name = "codr-vnet"
   location = "East US"
   address_space = ["10.0.0.0/16"]
   resource_group_name = azurerm_resource_group.main.name
}

# EC2 instance in AWS

resource "aws_instance" "codr_ec2" {
   ami = "ami-0abcd1234abcd1234"
   instance_type = "t2.micro"
   region = "us-east-1"
}

# Virtual Machine in Azure 

resource "azurerm_linux_virtual_machine" "codr_vm" {
   name = "codr-azure-vm"
   resource_group_name = azurerm_resource_group.main.name
   location = azurerm_resource_group.main.location
   size = "Standard_B1s"
   admin_username = "azureuser"
   admin_password = "P@ssw0rd123!"
}

🔹 Kubernetes - Self-Healing with Liveness and Readliness Probes 
apiVersion: apps/v1
kind: Deployment
metadata:
  name: codr-web-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: codr-web-app
  template:
    metadata:
      labels:
         app: codr-web-app
    spec:
      containers:
         - name: nginx
           image: nginx
           ports:
             - containerPort: 80
           livenessProbe:
              httpGet:
                 path: /healthz
                 port: 80
              intialDelaySeconds: 5
              periodSeconds: 10
           readnessProbe:
              httpGet:
                 path: /healthz
                 port: 80
              initialDelaySeconds: 5
              periodSeconds: 10
            
🔹 Jenkinsfile - Rolling Deployment with Kubernetes

pipeine {
   agent any 
   environment {
       IMAGE_NAME = "codr-web-app"
   }
   stages {
      stage('Build') {
        steps {
           echo 'Building Docker image...'
           docker.build("{env.IMAGE_NAME}")
        }
      }
      stage('Test') {
        steps {
           echo 'Running tests...'
        }
      }
      stage('Deploy to Kubernetes') {
        steps {
           script {
             sh 'kubectl apply -f kubernetes/deployment.yaml'
             sh  'kubectl rollout status deployment/codr-web-app'
           }
        }
      }
   }
}

🔹 GitLab CI/CD - Multi-Cloud Deployment
stages:
  - build
  - push
  - deploy

build:
  stage: build
  script: 
    - docker build -t codr-web-app .

push:
  stage: push
  script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD"
    - docker push codr-web-app

deploy_aws:
  stage: deploy
  script:
    - kubctl --context=aws-cluster apply -f kubernets/deployment.yaml

deploy_azure:
  stage: deploy
  script:
    - kubectl --context=azure-cluster apply -f kubernetes/deployment.yaml

🔹 Docker

# MySQL Container (Note - Here " \" means to be continue - it can be written in a single line")
docker run -d \
--name codr-mysql \
-e MYSQL_ROOT_PASSWORD=root \
mysql:8

# Real Path
/var/lib/mysql