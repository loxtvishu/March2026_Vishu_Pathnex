# Day 06 - DEBUG DAY

Fix these broken files:

## 🔹 Ansible (broken)

- name: Install Nginx
  hosts: all
  become: yes
  
  tasks:
    - name: Install Nginx
    yum:
      name: nginx
      state: present

## 🔹 Terraform (broken)

resource "aws_instance" "BrokenEC2" {
    ami = "ami-0xyz"
    instance_type = "t2.micro"

    tags = {
        Name         = "Broken"
        Environment  = "Prod"
        Product      = "Server"
        Team         = "DevOps"
        Owner        = "Vishu"
    }
}

## 🔹 Kubernetes (broken)

apiVersion: v1
kind: Pod
metadata:
  name: pathnex
spec:
  containers:
    - name: app
      image: nginx

# Debugging Pipelines
## 🔹 Jenkins Pipeline - Debugging
You will learn how to **fix broken stages and handle errors**.

pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main',url: 'https://github.com/loxtvishu/sample-java-app.git'
            }
        }
        stage('Build') {
            steps {
                sh '''
                mvn clean compile || { echo "Compile failed"; exit 1; }
            }
        }
        stage('Test') {
            steps {
                sh '''
                mvn test || { echo "Tests failed"; exit 1;}
                '''
            }
        }
    }
}

## 🔹 GitLab CI - Debugging
You will learn how to **handle job failures and exit codes**.

stages:
  - build
  - test

build:
  stage: build
  image: maven:3.8.1-jdk-17
  script:
    - git clone https://github.com/loxtvishu/sample-java-app.git
    - cd sample-java-app
    - mvn clean compile || { echo "Compile failed"; exit 1; }

# Day 06 - Introduction to CloudFormation and Kubernetes Advanced Concepts

## 🔹 Ansible - Install Docker

```yaml
- name: Install Docker
  hosts: all
  become: yes
  
  tasks:
    - name: Install Docker
      yum:
        name: docker
        state: present
    - name: Start Docker
      service:
        name: docker
        state: started
        enabled: yes

🔹 Terraform - Provisioning EC2 with VPC
resource "aws_vpc" "main" {
   cidr_block = "10.0.0.0/16"
}

resource "aws_instance" "pathnex_ec2" {
   ami = "ami-0abcd1234abcd1234"
   instance_type = "t3.medium"
   subnet_id = aws_subnet.main.id

   tags = {
      Name = "Pathnex-EC2"
      Environment = "Prod"
      TEAM = "Plateform-Engineering"
      Product = "Server"
      Owner = "Vishu"
   }
}

🔹 Kubernetes - Pod With Volume Mount

apiVersion: v1
kind: Pod
metatada:
  name: pathnex-pod
spec:
  containers:
    - name: web
      image: nginx
      volumeMounts:
        - mountPath: /usr/share/nginx/html
          name: html-volume
  volumes:
    - name: html-volume
      hostPath:
        path: /data
        type: Directory


🔹 Shell Script - Check for Running Services

#!/bin/bash
services=("nginx" "docker" "httpd")
for service in "${services[@]}"
do
  if systemctl is-active --quite $services; then
     echo "$service is running"
  else
     echo "$service is not running"
  fi
done

🔹 Docker
# Node.js Application (JavaScript)
console.log("Hello Pathnex");

# Docker File
FROM node:18
WORKDIR /opt/loxtvishu/node-app
COPY app.js /opt/loxtvishu/node-app/
CMD ["node", "/opt/loxtvishu/node-app/app.js"]

# Real Path
/opt/loxtvishu/node-app